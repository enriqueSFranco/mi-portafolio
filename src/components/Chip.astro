---
export interface Props {
  color: string;
  bgColor: string;
  text: string;
}

const { text, color, bgColor } = Astro.props;
---

<span
  style={{ color, backgroundColor: bgColor }}
  class='text-sm font-medium me-2 px-2.5 py-0.5 rounded'>{text}</span
>
