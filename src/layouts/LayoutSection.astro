---
export interface Props {
  sectionTitle: string;
}

const { sectionTitle } = Astro.props;
---

<section
  class='w-full h-full text-sm font-light flex justify-start gap-4 text-white'
>
  <header>
    <h2 class='uppercase w-32 text-balance font-bold'>
      {sectionTitle}
    </h2>
  </header>
  <slot />
</section>
