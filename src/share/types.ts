export interface Project {
  name: string;
  description: string;
  skills: string[]
  repository: string;
  deploy: string;
}

export interface ItemMenu {
  title: string
  href: string
  icon: string
}